import React, { Component } from "react";

class Customers extends Component {
  render() {
    return (
      <div>
        <h1>Customers</h1>
        <button>Save</button>
      </div>
    );
  }
}

export default Customers;
